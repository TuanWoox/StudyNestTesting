import { Spin } from 'antd';

const Spinner = () => {
    return (
        <div className="flex items-center justify-center m-auto">
            <Spin size="large" />
        </div>
    )
};

export default Spinner;